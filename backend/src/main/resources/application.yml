spring:
  application:
    name: gis-platform-backend
  profiles:
    default: local
  jpa:
    open-in-view: false

springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  port: ${SERVER_PORT:8080}
